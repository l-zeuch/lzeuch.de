---
name: Deploy to dev

on:
  push:
    branches:
      - dev

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: appleboy/ssh-action@master
      with:
        host: ${{ secrets.HOST }}
        username: ${{ secrets.USERNAME }}
        key: ${{ secrets.KEY }}
        passphrase: ${{ secrets.PASSPHRASE }}
        port: ${{ secrets.PORT }}
        script: |
          cd website && git checkout dev
          git fetch origin -v && git reset --hard origin/dev
          git submodule update --recursive
          hugo
          rsync -avz --delete public/ /var/www/html/website-dev/
